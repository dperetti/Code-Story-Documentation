/**
 * Generic purpose items.
 * Used as a workaround to display items on the client (modal panel) when
 * something has changed asynchronously on the server.
 * @type {Mongo.Collection}
 */
ModalItems= new Meteor.Collection('modalItems');
